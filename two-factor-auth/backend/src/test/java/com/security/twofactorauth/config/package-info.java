/**
 * Test configuration package.
 * 
 * This package contains test-specific configurations:
 * 
 * 1. TestConfig:
 *    - Base test configuration
 *    - Common beans for testing
 *    - Test property sources
 * 
 * 2. TestSecurityConfig:
 *    - Security configuration for tests
 *    - JWT configuration
 *    - Authentication setup
 *    - Authorization rules
 * 
 * 3. TestMailConfig:
 *    - Email service configuration
 *    - Mock mail server setup
 *    - SMTP settings for testing
 * 
 * Key Components:
 * - Test property sources
 * - Mock beans
 * - Security settings
 * - Database configuration
 * - Mail server setup
 * 
 * Features:
 * - In-memory database
 * - Mock mail server
 * - Test-specific JWT settings
 * - Shortened timeouts
 * - Test-specific security rules
 * 
 * These configurations ensure tests run in a
 * controlled, isolated environment while maintaining
 * production-like behavior.
 */
package com.security.twofactorauth.config;
